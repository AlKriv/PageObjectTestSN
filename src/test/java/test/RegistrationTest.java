package test;

import org.junit.Test;
import page.RegistrationPage;

import static java.lang.Thread.sleep;

public class RegistrationTest extends BaseTest {
    RegistrationPage page;

/*
    @Test
    public void typeInvalid() throws InterruptedException {
        page = new RegistrationPage();
        page.openPage()
                .setEmail(user.getEmail())
                .setPassword(user.getPassword())
                .setConfirmPassword(user.getPassword())
                .clickNextFirst()
                .setName(user.getName())
                .setSurname(user.getSurname())
                .setPhone(user.getPhone1())
                .clickNextSecond()
                .setCompanyName(company.getCompany())
                .setIndustry()
                .setTypeOrganization()
                .setSite(company.getSite())
                .setAddress(company.getAddress())
                .setCity(company.getCity())
                .setCountry(company.getCountry())
                .setTelCompany(company.getPhone1())
                .setEmailCompany(company.getEmail())
                .setTerms(true);
        //.clickRegisterButton();
        sleep(10000);


    }
*/


}
